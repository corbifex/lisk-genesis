language: node_js
os:
  - linux
node_js:
  - "12"
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-7
before_install:
  - export CC=gcc-7 && CXX=g++-7; fi
install:
  - npm i -g codecov
  - yarn install
  - yarn run test:ci
  - codecov
